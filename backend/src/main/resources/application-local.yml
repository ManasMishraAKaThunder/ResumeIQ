spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/resumeiq
    username: resumeiq
    password: resumeiq_dev_password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  data:
    redis:
      host: localhost
      port: 6379